apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zot-data
  namespace: modelops-zot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: gp3-csi
