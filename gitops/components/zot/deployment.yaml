apiVersion: apps/v1
kind: Deployment
metadata:
  name: zot
  namespace: modelops-zot
  labels:
    app: zot
spec:
  replicas: 1
  # Recreate, not RollingUpdate: the registry stores a boltdb cache.db on the
  # RWO zot-data PVC, and two pods cannot open the same boltdb file
  # ("boltdb file is already in use"). Recreate terminates the old pod before
  # the new one starts, which is the correct strategy for this single-replica,
  # PVC-backed registry.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: zot
  template:
    metadata:
      labels:
        app: zot
    spec:
      containers:
        - name: zot
          image: ghcr.io/project-zot/zot:v2.1.20
          # The image's default entrypoint already runs `zot serve /etc/zot/config.json`,
          # which reads the ConfigMap-mounted config below.
          ports:
            - containerPort: 5000
              name: http
          volumeMounts:
            - name: data
              mountPath: /var/lib/registry
            - name: config
              mountPath: /etc/zot/config.json
              subPath: config.json
            - name: htpasswd
              mountPath: /etc/zot/htpasswd
              subPath: htpasswd
            - name: certs
              mountPath: /etc/zot/certs
              readOnly: true
          # Zot serves TLS on 5000 now (config.json's http.tls). A tcpSocket
          # probe checks only that the port is accepting connections, so it
          # works regardless of TLS and avoids forcing kubelet to verify the
          # service-CA-signed cert (node-level trust that lives behind the
          # deferred additionalTrustedCA decision, not this change).
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 1
              memory: 1Gi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: zot-data
        - name: config
          configMap:
            name: zot-config
            items:
              - key: config.json
                path: config.json
        - name: htpasswd
          secret:
            secretName: zot-htpasswd
        - name: certs
          secret:
            secretName: zot-serving-cert
